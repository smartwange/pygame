1、循环引用，课程 中有学员信息，学员中有对课程的收藏和评论。这种引入层级设计，通过operations应用来处理
   层级中，最顶层的可以调用任何其他的应用，下层不能调用上层，
   用户是最底层的应用，operations最顶层，课程和组织平级
2、一个实体一张表，课程中出现一对多的关系，就应该再建一张表
   实体1 <关系> 实体2
   课程 中有多个 章节 有多个 视频  课程中有多个 课程资源

3、基于顶层设计，Users应用被其他的引用，那么，基类应该在users.model中定义